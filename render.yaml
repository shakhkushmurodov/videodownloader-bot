services:
  - type: web
    name: video-downloader-bot
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PORT
        value: 3000
    disk:
      name: bot-data
      mountPath: /app/downloads
      sizeGB: 1
